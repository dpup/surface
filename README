JS library for managing "AJAXy" applications.  Will add better documentation eventually, this was a git subtree split so that I can use it in multiple other projects.

Subtree change.  And again. Once more from a subtree.
